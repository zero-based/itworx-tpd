overwrite: true
schema: "http://localhost:4000/graphql"
documents: "graphql/**/*.gql"
generates:
  generated/graphql.tsx:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-urql"
