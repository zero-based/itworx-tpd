overwrite: true
schema: "http://localhost:4000/graphql"
documents: "graphql/**/*.gql"
generates:
  graphql/types/index.tsx:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-urql"
